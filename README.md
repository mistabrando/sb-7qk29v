# Private Safari Stream Browser

Deploy this folder as a static site. The stream catalog is encrypted in `encrypted-catalog.json`.

Do not commit `catalog-data.js`; it contains plaintext stream URLs.
